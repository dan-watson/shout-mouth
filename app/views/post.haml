= partial(:'shared/_post', :article => @article)

%div.comments
	- @article.comments.all_active_and_ham.each do |comment|
		= partial(:'shared/_comment', :comment => comment)

-if @article.allow_comments?
	= partial(:'shared/_comment_form', :post => @article)
					

				
				
				
	
	